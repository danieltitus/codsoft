/* General Reset */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

body {
    font-family: Arial, sans-serif;
    line-height: 1.6;
    background-color: #000; /* Set background color to black */
    color: #f4f4f4; /* Change text color to a light color for contrast */
}

/* Header Styles */
header {
    background: #007BFF;
    color: white;
    padding: 20px 0;
    text-align: center;
    animation: fadeIn 1s ease-in;
}

/* Navigation Styles */
nav {
    background: #333;
    color: white;
    padding: 10px 0;
}

nav ul {
    list-style: none;
    display: flex;
    justify-content: center;
}

nav ul li {
    margin: 0 15px;
}

nav ul li a {
    color: white;
    text-decoration: none;
    transition: color 0.3s;
}

nav ul li a:hover {
    color: #007BFF;
}

/* Photo Section Styles */
#photo {
    text-align: center;
    margin: 20px;
}

.photo-animation {
    width: 250px; /* Set width to 250 pixels */
    height: auto; /* Maintain aspect ratio */
    max-height: 200px; /* Set a maximum height to prevent it from being too tall */
    object-fit: contain; /* Ensures the entire image is visible without cropping */
    opacity: 0; /* Start invisible */
    animation: fadeIn 2s forwards; /* Apply fade-in animation */
}

@keyframes fadeIn {
    from {
        opacity: 0; /* Start from transparent */
    }
    to {
        opacity: 1; /* End fully visible */
    }
}

/* Section Styles */
section {
    padding: 20px;
    background: #222; /* Dark background for sections */
    margin: 20px;
    border-radius: 8px;
    box-shadow: 0 2px 10px rgba(255, 255, 255, 0.1); /* Light shadow for contrast */
    animation: slideIn 0.5s ease-in-out;
}

h2 {
    color: #007BFF; /* Change heading color */
    margin-bottom: 10px;
}

/* Skills Section Styles */
#skills ul {
    list-style-type: disc;
    padding-left: 20px;
}

/* Experience Section Styles */
#experience ul {
    list-style-type: disc;
    padding-left: 20px;
}

/* Certifications Section Styles */
#certifications ul {
    list-style-type: disc;
    padding-left: 20px;
}

/* Contact Section Styles */
#contact p {
    margin: 5px 0; /* Space between paragraphs */
}

/* Footer Styles */
footer {
    text-align: center;
    padding: 10px 0;
    background: #333;
    color: white;
}

/* Animations */
@keyframes slideIn {
    from {
        transform: translateY(20px);
        opacity: 0;
    }
    to {
        transform: translateY(0);
        opacity: 1;
    }
}
